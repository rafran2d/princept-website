import React from 'react';
import { Twitter, Facebook, Instagram, Linkedin, Youtube, Github, Mail, Phone, MapPin, ArrowUp, Heart, Globe, Star, Users, Award, Shield, Zap, Target, TrendingUp, Code, Briefcase } from 'lucide-react';
import SocialLinks from '../SocialLinks';

// GitHub Footer - Developer-focused
export const GitHubFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  const footerSections = [
    {
      title: "Navigation",
      links: navigationItems?.map(item => ({
        label: item.label,
        onClick: () => scrollToSection?.(item.type)
      })) || []
    },
    {
      title: "Contact",
      links: [
        settings?.email && { label: settings.email, href: `mailto:${settings.email}` },
        settings?.phone && { label: settings.phone, href: `tel:${settings.phone}` },
        { label: "Administration", href: "/admin" }
      ].filter(Boolean)
    }
  ];

  return (
    <footer 
      className="border-t"
      style={{ 
        backgroundColor: theme.colors.surface,
        borderColor: theme.colors.text.light + '20'
      }}
    >
      <div className="container mx-auto px-6 py-16">
        <div className="grid md:grid-cols-5 gap-8">
          <div className="md:col-span-1">
            {settings?.logoUrl ? (
              <img 
                src={settings.logoUrl} 
                alt={settings.siteName || 'Logo'} 
                className="h-8 w-auto object-contain mb-4"
              />
            ) : (
              <div 
                className="text-2xl font-bold mb-4"
                style={{ color: theme.colors.text.primary }}
              >
                {settings?.logoText || ''}
              </div>
            )}
            <p 
              className="text-sm mb-6"
              style={{ color: theme.colors.text.secondary }}
            >
              {settings?.footerText || settings?.siteDescription || ''}
            </p>
            <SocialLinks 
              customStyles={true}
              size={20}
              iconStyle={{ color: theme.colors.text.secondary }}
            />
          </div>

          {footerSections.map((section, index) => (
            <div key={index}>
              <h3 
                className="font-semibold mb-4"
                style={{ color: theme.colors.text.primary }}
              >
                {section.title}
              </h3>
              <ul className="space-y-3">
                {section.links.map((link, linkIndex) => (
                  <li key={linkIndex}>
                    {link.onClick ? (
                      <button 
                        onClick={link.onClick}
                        className="text-sm hover:underline text-left"
                        style={{ color: theme.colors.text.secondary }}
                      >
                        {link.label}
                      </button>
                    ) : (
                      <a 
                        href={link.href || "#"}
                        className="text-sm hover:underline"
                        style={{ color: theme.colors.text.secondary }}
                      >
                        {link.label}
                      </a>
                    )}
                  </li>
                ))}
              </ul>
            </div>
          ))}
        </div>

        <div 
          className="border-t pt-8 mt-12 flex flex-col md:flex-row justify-between items-center"
          style={{ borderColor: theme.colors.text.light + '20' }}
        >
          <div className="flex flex-wrap gap-6 mb-4 md:mb-0">
            <p 
              className="text-sm"
              style={{ color: theme.colors.text.secondary }}
            >
              © {new Date().getFullYear()} {settings?.siteName}
            </p>
          </div>
        </div>
      </div>
    </footer>
  );
};

// Airbnb Footer - Clean hospitality style
export const AirbnbFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  const footerSections = [
    {
      title: "Navigation",
      links: navigationItems?.map(item => ({
        label: item.label,
        onClick: () => scrollToSection?.(item.type)
      })) || []
    },
    {
      title: "Contact",
      links: [
        settings?.email && { label: settings.email, href: `mailto:${settings.email}` },
        settings?.phone && { label: settings.phone, href: `tel:${settings.phone}` },
        settings?.address && { label: "Adresse", href: "#" }
      ].filter(Boolean)
    },
    {
      title: "Informations",
      links: [
        { label: "Administration", href: "/admin" }
      ]
    }
  ];

  return (
    <footer 
      className="border-t"
      style={{ 
        backgroundColor: theme.colors.surface,
        borderColor: theme.colors.text.light + '20'
      }}
    >
      <div className="container mx-auto px-6 py-16">
        <div className="grid md:grid-cols-3 gap-8">
          {footerSections.map((section, index) => (
            <div key={index}>
              <h3 
                className="font-semibold mb-4"
                style={{ color: theme.colors.text.primary }}
              >
                {section.title}
              </h3>
              <ul className="space-y-3">
                {section.links.map((link, linkIndex) => (
                  <li key={linkIndex}>
                    {link.onClick ? (
                      <button 
                        onClick={link.onClick}
                        className="text-sm hover:underline text-left"
                        style={{ color: theme.colors.text.secondary }}
                      >
                        {link.label}
                      </button>
                    ) : (
                      <a 
                        href={link.href || "#"}
                        className="text-sm hover:underline"
                        style={{ color: theme.colors.text.secondary }}
                      >
                        {link.label}
                      </a>
                    )}
                  </li>
                ))}
              </ul>
            </div>
          ))}
        </div>

        <div 
          className="border-t pt-8 mt-12 flex flex-col md:flex-row justify-between items-center"
          style={{ borderColor: theme.colors.text.light + '20' }}
        >
          <div className="flex flex-wrap gap-6 mb-4 md:mb-0">
            <p 
              className="text-sm"
              style={{ color: theme.colors.text.secondary }}
            >
              © {new Date().getFullYear()} {settings?.siteName}
            </p>
          </div>
          
          <div className="flex items-center gap-4">
            <SocialLinks 
              customStyles={true}
              size={20}
              iconStyle={{ color: theme.colors.text.secondary }}
            />
          </div>
        </div>
      </div>
    </footer>
  );
};

// Spotify Footer - Dark music platform style
export const SpotifyFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  const footerSections = [
    {
      title: "Navigation",
      links: navigationItems?.map(item => ({
        label: item.label,
        onClick: () => scrollToSection?.(item.type)
      })) || []
    },
    {
      title: "Contact",
      links: [
        settings?.email && { label: settings.email, href: `mailto:${settings.email}` },
        settings?.phone && { label: settings.phone, href: `tel:${settings.phone}` }
      ].filter(Boolean)
    },
    {
      title: "Liens utiles",
      links: [
        { label: "Administration", href: "/admin" }
      ]
    }
  ];

  return (
    <footer 
      style={{ 
        backgroundColor: theme.colors.background,
        color: theme.colors.text.primary
      }}
    >
      <div className="container mx-auto px-6 py-16">
        <div className="grid md:grid-cols-5 gap-8">
          <div className="md:col-span-2">
            {settings?.logoUrl ? (
              <img 
                src={settings.logoUrl} 
                alt={settings.siteName || 'Logo'} 
                className="h-10 w-auto object-contain mb-8"
              />
            ) : (
              <div 
                className="text-3xl font-bold mb-8"
                style={{ color: theme.colors.text.primary }}
              >
                {settings?.logoText || ''}
              </div>
            )}
          </div>

          {footerSections.map((section, index) => (
            <div key={index}>
              <h3 
                className="font-bold mb-6"
                style={{ color: theme.colors.text.primary }}
              >
                {section.title}
              </h3>
              <ul className="space-y-4">
                {section.links.map((link, linkIndex) => (
                  <li key={linkIndex}>
                    {link.onClick ? (
                      <button 
                        onClick={link.onClick}
                        className="hover:underline hover:opacity-70 transition-opacity text-left"
                        style={{ color: theme.colors.text.secondary }}
                      >
                        {link.label}
                      </button>
                    ) : (
                      <a 
                        href={link.href || "#"}
                        className="hover:underline hover:opacity-70 transition-opacity"
                        style={{ color: theme.colors.text.secondary }}
                      >
                        {link.label}
                      </a>
                    )}
                  </li>
                ))}
              </ul>
            </div>
          ))}
        </div>

        <div className="flex justify-end mt-16 mb-12">
          <SocialLinks 
            circularStyle={true}
            size={20}
            iconStyle={{ color: theme.colors.background }}
            circularBgStyle={{ backgroundColor: theme.colors.text.secondary }}
          />
        </div>

        <div 
          className="border-t pt-8"
          style={{ borderColor: theme.colors.text.light }}
        >
          <div className="flex flex-col md:flex-row justify-between items-center">
            <p 
              className="text-xs"
              style={{ color: theme.colors.text.light }}
            >
              © {new Date().getFullYear()} {settings?.siteName}
            </p>
          </div>
        </div>
      </div>
    </footer>
  );
};

// Default Footer - Simple et moderne
export const DefaultFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  return (
    <footer className="bg-gray-900 text-white py-16">
      <div className="container mx-auto px-6">
        <div className="grid md:grid-cols-4 gap-8">
          <div className="md:col-span-2">
            <h3 className="text-2xl font-bold mb-4" style={{ color: theme.colors.primary }}>
              {settings?.siteName}
            </h3>
            <p className="text-gray-400 mb-6">
              {settings?.footerText || settings?.siteDescription || ''}
            </p>
            <SocialLinks 
              customStyles={true}
              size={20}
              iconStyle={{ color: '#9ca3af' }}
              linkClassName="text-gray-400 hover:text-white cursor-pointer"
            />
          </div>
          <div>
            <h4 className="font-semibold mb-4">Navigation</h4>
            {navigationItems?.map((item) => (
              <button key={item.id} onClick={() => scrollToSection?.(item.type)} className="block text-gray-400 hover:text-white mb-2">
                {item.label}
              </button>
            ))}
          </div>
          <div>
            <h4 className="font-semibold mb-4">Contact</h4>
            {settings?.email && <p className="text-gray-400 mb-2">{settings.email}</p>}
            {settings?.phone && <p className="text-gray-400 mb-2">{settings.phone}</p>}
            <a href="/admin" className="block text-gray-400 hover:text-white">Admin</a>
          </div>
        </div>
        <div className="border-t border-gray-800 mt-8 pt-8 text-center text-gray-400">
          <p>© {new Date().getFullYear()} {settings?.siteName}</p>
        </div>
      </div>
    </footer>
  );
};

// Slack Footer - Corporate style
export const SlackFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  return (
    <footer className="border-t-4" style={{ borderColor: theme.colors.primary, backgroundColor: theme.colors.surface }}>
      <div className="container mx-auto px-6 py-12">
        <div className="grid md:grid-cols-5 gap-8">
          <div className="md:col-span-2">
            <div className="flex items-center gap-3 mb-6">
              {settings?.logoUrl ? (
                <img 
                  src={settings.logoUrl} 
                  alt={settings.siteName || 'Logo'} 
                  className="h-10 w-auto object-contain"
                />
              ) : (
                <div className="w-10 h-10 rounded" style={{ backgroundColor: theme.colors.primary }}>
                  <span className="text-white font-bold text-lg flex items-center justify-center h-full">
                    {settings?.siteName ? settings.siteName.charAt(0) : ''}
                  </span>
                </div>
              )}
              <h3 className="text-2xl font-bold" style={{ color: theme.colors.text.primary }}>
                {settings?.logoText || ''}
              </h3>
            </div>
            <p className="mb-6" style={{ color: theme.colors.text.secondary }}>
              {settings?.footerText || ''}
            </p>
            <SocialLinks 
              customStyles={true}
              size={20}
              iconStyle={{ color: theme.colors.text.secondary }}
              linkClassName="w-8 h-8 p-2 rounded hover:bg-gray-100 cursor-pointer"
              className="gap-3"
            />
          </div>

          <div>
            <h4 className="font-semibold mb-4" style={{ color: theme.colors.text.primary }}>Navigation</h4>
            <ul className="space-y-2">
              {navigationItems?.map((item) => (
                <li key={item.id}>
                  <button 
                    onClick={() => scrollToSection?.(item.type)} 
                    className="hover:underline text-left"
                    style={{ color: theme.colors.text.secondary }}
                  >
                    {item.label}
                  </button>
                </li>
              ))}
            </ul>
          </div>

          <div>
            <h4 className="font-semibold mb-4" style={{ color: theme.colors.text.primary }}>Contact</h4>
            <ul className="space-y-2">
              {settings?.email && (
                <li>
                  <a href={`mailto:${settings.email}`} className="hover:underline" style={{ color: theme.colors.text.secondary }}>
                    {settings.email}
                  </a>
                </li>
              )}
              {settings?.phone && (
                <li>
                  <a href={`tel:${settings.phone}`} className="hover:underline" style={{ color: theme.colors.text.secondary }}>
                    {settings.phone}
                  </a>
                </li>
              )}
              <li>
                <a href="/admin" className="hover:underline" style={{ color: theme.colors.text.secondary }}>
                  Administration
                </a>
              </li>
            </ul>
          </div>
        </div>

        <div className="border-t pt-6 mt-12" style={{ borderColor: theme.colors.text.light + '30' }}>
          <div className="flex flex-col md:flex-row justify-between items-center">
            <p className="text-sm" style={{ color: theme.colors.text.secondary }}>
              © {new Date().getFullYear()} {settings?.siteName}
            </p>
          </div>
        </div>
      </div>
    </footer>
  );
};

// Notion Footer - Clean productivity style
export const NotionFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  return (
    <footer className="border-t" style={{ backgroundColor: theme.colors.surface, borderColor: theme.colors.text.light + '20' }}>
      <div className="container mx-auto px-6 py-12">
        <div className="grid md:grid-cols-4 gap-8">
          <div className="md:col-span-2">
            <div className="mb-6">
              <h3 className="text-2xl font-bold mb-4" style={{ color: theme.colors.text.primary }}>
                {settings?.logoText || ''}
              </h3>
              <p className="text-sm" style={{ color: theme.colors.text.secondary }}>
                {settings?.footerText || ''}
              </p>
            </div>
            <SocialLinks customStyles={true} size={20} iconStyle={{ color: theme.colors.text.secondary }} />
          </div>

          <div>
            <h4 className="font-medium mb-4" style={{ color: theme.colors.text.primary }}>Navigation</h4>
            <ul className="space-y-3">
              {navigationItems?.map((item) => (
                <li key={item.id}>
                  <button 
                    onClick={() => scrollToSection?.(item.type)} 
                    className="text-sm hover:underline text-left"
                    style={{ color: theme.colors.text.secondary }}
                  >
                    {item.label}
                  </button>
                </li>
              ))}
            </ul>
          </div>

          <div>
            <h4 className="font-medium mb-4" style={{ color: theme.colors.text.primary }}>Contact</h4>
            <ul className="space-y-3">
              {settings?.email && (
                <li>
                  <a href={`mailto:${settings.email}`} className="text-sm hover:underline" style={{ color: theme.colors.text.secondary }}>
                    {settings.email}
                  </a>
                </li>
              )}
              {settings?.phone && (
                <li>
                  <a href={`tel:${settings.phone}`} className="text-sm hover:underline" style={{ color: theme.colors.text.secondary }}>
                    {settings.phone}
                  </a>
                </li>
              )}
              <li>
                <a href="/admin" className="text-sm hover:underline" style={{ color: theme.colors.text.secondary }}>
                  Administration
                </a>
              </li>
            </ul>
          </div>
        </div>

        <div className="border-t pt-6 mt-8" style={{ borderColor: theme.colors.text.light + '20' }}>
          <p className="text-xs" style={{ color: theme.colors.text.light }}>
            © {new Date().getFullYear()} {settings?.siteName}
          </p>
        </div>
      </div>
    </footer>
  );
};

// Stripe Footer - Payment focused style
export const StripeFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  return (
    <footer style={{ backgroundColor: theme.colors.surface }}>
      <div className="container mx-auto px-6 py-16">
        <div className="grid md:grid-cols-6 gap-8">
          <div className="md:col-span-2">
            <div className="mb-8">
              <h3 className="text-2xl font-bold mb-4" style={{ color: theme.colors.text.primary }}>
                {settings?.logoText || ''}
              </h3>
              <p className="text-sm" style={{ color: theme.colors.text.secondary }}>
                {settings?.footerText || ''}
              </p>
            </div>
            <SocialLinks customStyles={true} size={20} iconStyle={{ color: theme.colors.text.secondary }} />
          </div>

          <div>
            <h4 className="font-medium mb-6" style={{ color: theme.colors.text.primary }}>Contact</h4>
            <ul className="space-y-3">
              {settings?.email && (
                <li>
                  <a href={`mailto:${settings.email}`} className="text-sm hover:opacity-70" style={{ color: theme.colors.text.secondary }}>
                    {settings.email}
                  </a>
                </li>
              )}
              {settings?.phone && (
                <li>
                  <a href={`tel:${settings.phone}`} className="text-sm hover:opacity-70" style={{ color: theme.colors.text.secondary }}>
                    {settings.phone}
                  </a>
                </li>
              )}
              <li>
                <a href="/admin" className="text-sm hover:opacity-70" style={{ color: theme.colors.text.secondary }}>
                  Administration
                </a>
              </li>
            </ul>
          </div>
        </div>

        <div className="border-t pt-8 mt-12" style={{ borderColor: theme.colors.text.light + '20' }}>
          <div className="flex flex-col md:flex-row justify-between items-center">
            <p className="text-sm" style={{ color: theme.colors.text.secondary }}>
              © {new Date().getFullYear()} {settings?.siteName}
            </p>
          </div>
        </div>
      </div>
    </footer>
  );
};

// Figma Footer - Design tool style
export const FigmaFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  return (
    <footer className="bg-black text-white">
      <div className="container mx-auto px-6 py-16">
        <div className="grid md:grid-cols-5 gap-8 mb-12">
          <div className="md:col-span-2">
            <h3 className="text-3xl font-bold mb-6" style={{ color: theme.colors.primary }}>
              {settings?.logoText || ''}
            </h3>
            <p className="text-gray-300 mb-8">
              {settings?.footerText || ''}
            </p>
            <div className="flex gap-4">
<SocialLinks customStyles={true} size={20} iconStyle={{ color: theme.colors.text.secondary }} />
              {navigationItems?.map((item) => (
                <li key={item.id}>
                  <button 
                    onClick={() => scrollToSection?.(item.type)} 
                    className="text-gray-400 hover:text-white transition-colors text-left"
                  >
                    {item.label}
                  </button>
                </li>
              ))}
            </ul>
          </div>

          <div>
            <h4 className="font-semibold mb-6 text-white">Contact</h4>
            <ul className="space-y-4">
              {settings?.email && (
                <li>
                  <a href={`mailto:${settings.email}`} className="text-gray-400 hover:text-white transition-colors">
                    {settings.email}
                  </a>
                </li>
              )}
              {settings?.phone && (
                <li>
                  <a href={`tel:${settings.phone}`} className="text-gray-400 hover:text-white transition-colors">
                    {settings.phone}
                  </a>
                </li>
              )}
              <li>
                <a href="/admin" className="text-gray-400 hover:text-white transition-colors">
                  Administration
                </a>
              </li>
            </ul>
          </div>
        </div>

        <div className="border-t border-gray-800 pt-8">
          <div className="flex flex-col md:flex-row justify-between items-center">
            <p className="text-gray-400">
              © {new Date().getFullYear()} {settings?.siteName}.
            </p>
          </div>
        </div>
      </div>
    </footer>
  );
};

// Discord Footer - Gaming/community style
export const DiscordFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  return (
    <footer className="bg-gray-900 text-white">
      <div className="container mx-auto px-6 py-16">
        <div className="grid md:grid-cols-5 gap-8 mb-16">
          <div className="md:col-span-2">
            <h3 className="text-4xl font-bold mb-6" style={{ color: theme.colors.primary }}>
              {settings?.logoText || ''}
            </h3>
            <p className="text-gray-300 text-lg mb-8">
              {settings?.footerText || ''}
            </p>
            <div className="flex gap-4">
<SocialLinks customStyles={true} size={20} iconStyle={{ color: theme.colors.text.secondary }} />
              {navigationItems?.map((item) => (
                <li key={item.id}>
                  <button 
                    onClick={() => scrollToSection?.(item.type)} 
                    className="text-gray-400 hover:text-white transition-colors text-left"
                  >
                    {item.label}
                  </button>
                </li>
              ))}
            </ul>
          </div>

          <div>
            <h4 className="font-semibold mb-6 text-white">Contact</h4>
            <ul className="space-y-3">
              {settings?.email && (
                <li>
                  <a href={`mailto:${settings.email}`} className="text-gray-400 hover:text-white transition-colors">
                    {settings.email}
                  </a>
                </li>
              )}
              {settings?.phone && (
                <li>
                  <a href={`tel:${settings.phone}`} className="text-gray-400 hover:text-white transition-colors">
                    {settings.phone}
                  </a>
                </li>
              )}
              <li>
                <a href="/admin" className="text-gray-400 hover:text-white transition-colors">
                  Administration
                </a>
              </li>
            </ul>
          </div>
        </div>

        <div className="border-t border-gray-800 pt-8">
          <div className="flex flex-col md:flex-row justify-between items-center">
            <p className="text-gray-400">
              © {new Date().getFullYear()} {settings?.siteName}.
            </p>
          </div>
        </div>
      </div>
    </footer>
  );
};

// LinkedIn Footer - Professional network style
export const LinkedInFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  return (
    <footer className="bg-gray-100" style={{ backgroundColor: theme.colors.surface }}>
      <div className="container mx-auto px-6 py-16">
        <div className="grid md:grid-cols-6 gap-8 mb-12">
          <div className="md:col-span-2">
            <div className="flex items-center gap-3 mb-6">
              {settings?.logoUrl ? (
                <img 
                  src={settings.logoUrl} 
                  alt={settings.siteName || 'Logo'} 
                  className="h-12 w-auto object-contain"
                />
              ) : (
                <div className="w-12 h-12 rounded" style={{ backgroundColor: theme.colors.primary }}>
                  <span className="text-white font-bold text-xl flex items-center justify-center h-full">
                    {settings?.siteName ? settings.siteName.substring(0, 2) : ''}
                  </span>
                </div>
              )}
              <h3 className="text-2xl font-bold" style={{ color: theme.colors.text.primary }}>
                {settings?.logoText || ''}
              </h3>
            </div>
            <p className="mb-8" style={{ color: theme.colors.text.secondary }}>
              {settings?.footerText || ''}
            </p>
            <div className="flex gap-4">
<SocialLinks customStyles={true} size={20} iconStyle={{ color: theme.colors.text.secondary }} />
              {navigationItems?.map((item) => (
                <li key={item.id}>
                  <button 
                    onClick={() => scrollToSection?.(item.type)} 
                    className="text-sm hover:underline text-left"
                    style={{ color: theme.colors.text.secondary }}
                  >
                    {item.label}
                  </button>
                </li>
              ))}
            </ul>
          </div>

          <div>
            <h4 className="font-semibold mb-4" style={{ color: theme.colors.text.primary }}>Contact</h4>
            <ul className="space-y-3">
              {settings?.email && (
                <li>
                  <a href={`mailto:${settings.email}`} className="text-sm hover:underline" style={{ color: theme.colors.text.secondary }}>
                    {settings.email}
                  </a>
                </li>
              )}
              {settings?.phone && (
                <li>
                  <a href={`tel:${settings.phone}`} className="text-sm hover:underline" style={{ color: theme.colors.text.secondary }}>
                    {settings.phone}
                  </a>
                </li>
              )}
              <li>
                <a href="/admin" className="text-sm hover:underline" style={{ color: theme.colors.text.secondary }}>
                  Administration
                </a>
              </li>
            </ul>
          </div>
        </div>

        <div className="border-t pt-8" style={{ borderColor: theme.colors.text.light + '30' }}>
          <div className="flex flex-col md:flex-row justify-between items-center">
            <p className="text-sm" style={{ color: theme.colors.text.secondary }}>
              © {new Date().getFullYear()} {settings?.siteName}
            </p>
          </div>
        </div>
      </div>
    </footer>
  );
};

// Shopify Footer - E-commerce platform style
export const ShopifyFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  return (
    <footer className="bg-gray-50" style={{ backgroundColor: theme.colors.surface }}>
      <div className="container mx-auto px-6 py-16">
        <div className="grid md:grid-cols-5 gap-8 mb-12">
          <div className="md:col-span-2">
            <h3 className="text-3xl font-bold mb-6" style={{ color: theme.colors.primary }}>
              {settings?.logoText || ''}
            </h3>
            <p className="mb-8" style={{ color: theme.colors.text.secondary }}>
              {settings?.footerText || ''}
            </p>
            <div className="flex gap-4">
<SocialLinks customStyles={true} size={20} iconStyle={{ color: theme.colors.text.secondary }} />
              {navigationItems?.map((item) => (
                <li key={item.id}>
                  <button 
                    onClick={() => scrollToSection?.(item.type)} 
                    className="hover:underline text-left"
                    style={{ color: theme.colors.text.secondary }}
                  >
                    {item.label}
                  </button>
                </li>
              ))}
            </ul>
          </div>

          <div>
            <h4 className="font-semibold mb-6" style={{ color: theme.colors.text.primary }}>Contact</h4>
            <ul className="space-y-3">
              {settings?.email && (
                <li>
                  <a href={`mailto:${settings.email}`} className="hover:underline" style={{ color: theme.colors.text.secondary }}>
                    {settings.email}
                  </a>
                </li>
              )}
              {settings?.phone && (
                <li>
                  <a href={`tel:${settings.phone}`} className="hover:underline" style={{ color: theme.colors.text.secondary }}>
                    {settings.phone}
                  </a>
                </li>
              )}
              <li>
                <a href="/admin" className="hover:underline" style={{ color: theme.colors.text.secondary }}>
                  Administration
                </a>
              </li>
            </ul>
          </div>
        </div>

        <div className="border-t pt-8" style={{ borderColor: theme.colors.text.light + '30' }}>
          <p className="text-center" style={{ color: theme.colors.text.secondary }}>
            © {new Date().getFullYear()} {settings?.siteName}.
          </p>
        </div>
      </div>
    </footer>
  );
};

// Netflix Footer - Streaming platform style
export const NetflixFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  return (
    <footer className="bg-black text-white py-16">
      <div className="container mx-auto px-6">
        <div className="grid md:grid-cols-4 gap-8 mb-12">
          <div className="md:col-span-2">
            <h3 className="text-4xl font-bold mb-6" style={{ color: theme.colors.primary }}>
              {settings?.logoText || ''}
            </h3>
            <p className="text-gray-300 text-lg mb-8">
              {settings?.footerText || ''}
            </p>
            <div className="flex gap-4">
<SocialLinks customStyles={true} size={20} iconStyle={{ color: theme.colors.text.secondary }} />
              <button key={item.id} onClick={() => scrollToSection?.(item.type)} className="block text-gray-400 hover:text-white mb-3 transition-colors">
                {item.label}
              </button>
            ))}
          </div>

          <div>
            <h4 className="font-medium mb-4 text-gray-300">Contact</h4>
            {settings?.email && (
              <a href={`mailto:${settings.email}`} className="block text-gray-400 hover:text-white mb-3 transition-colors">
                {settings.email}
              </a>
            )}
            {settings?.phone && (
              <a href={`tel:${settings.phone}`} className="block text-gray-400 hover:text-white mb-3 transition-colors">
                {settings.phone}
              </a>
            )}
            <a href="/admin" className="block text-gray-400 hover:text-white transition-colors">
              Administration
            </a>
          </div>
        </div>

        <div className="border-t border-gray-800 pt-8">
          <p className="text-center text-gray-400">
            © {new Date().getFullYear()} {settings?.siteName}
          </p>
        </div>
      </div>
    </footer>
  );
};

// Apple Footer - Minimal premium style
export const AppleFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  return (
    <footer style={{ backgroundColor: theme.colors.surface }}>
      <div className="container mx-auto px-6 py-12">
        <div className="grid md:grid-cols-4 gap-8 mb-8">
          <div>
            <h4 className="font-medium mb-4" style={{ color: theme.colors.text.primary }}>Navigation</h4>
            {navigationItems?.map((item) => (
              <button key={item.id} onClick={() => scrollToSection?.(item.type)} className="block mb-2 text-sm hover:underline text-left" style={{ color: theme.colors.text.secondary }}>
                {item.label}
              </button>
            ))}
          </div>
          
          <div>
            <h4 className="font-medium mb-4" style={{ color: theme.colors.text.primary }}>Contact</h4>
            {settings?.email && (
              <a href={`mailto:${settings.email}`} className="block text-sm hover:underline mb-2" style={{ color: theme.colors.text.secondary }}>
                {settings.email}
              </a>
            )}
            {settings?.phone && (
              <a href={`tel:${settings.phone}`} className="block text-sm hover:underline mb-2" style={{ color: theme.colors.text.secondary }}>
                {settings.phone}
              </a>
            )}
            <a href="/admin" className="block text-sm hover:underline" style={{ color: theme.colors.text.secondary }}>
              Administration
            </a>
          </div>
        </div>

        <div className="border-t pt-6" style={{ borderColor: theme.colors.text.light + '30' }}>
          <div className="flex flex-col md:flex-row justify-between items-center">
            <p className="text-xs mb-4 md:mb-0" style={{ color: theme.colors.text.light }}>
              © {new Date().getFullYear()} {settings?.siteName}
            </p>
          </div>
        </div>
      </div>
    </footer>
  );
};

// Dribbble Footer - Creative community style
export const DribbbleFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  return (
    <footer style={{ backgroundColor: theme.colors.surface }}>
      <div className="container mx-auto px-6 py-16">
        <div className="grid md:grid-cols-5 gap-8 mb-12">
          <div className="md:col-span-2">
            <h3 className="text-3xl font-bold mb-6" style={{ color: theme.colors.primary }}>
              {settings?.logoText || ''}
            </h3>
            <p className="mb-8" style={{ color: theme.colors.text.secondary }}>
              {settings?.footerText || ''}
            </p>
            <div className="flex gap-4">
<SocialLinks customStyles={true} size={20} iconStyle={{ color: theme.colors.text.secondary }} />
              {navigationItems?.map((item) => (
                <li key={item.id}>
                  <button 
                    onClick={() => scrollToSection?.(item.type)} 
                    className="hover:underline text-left"
                    style={{ color: theme.colors.text.secondary }}
                  >
                    {item.label}
                  </button>
                </li>
              ))}
            </ul>
          </div>

          <div>
            <h4 className="font-semibold mb-6" style={{ color: theme.colors.text.primary }}>Contact</h4>
            <ul className="space-y-3">
              {settings?.email && (
                <li>
                  <a href={`mailto:${settings.email}`} className="hover:underline" style={{ color: theme.colors.text.secondary }}>
                    {settings.email}
                  </a>
                </li>
              )}
              {settings?.phone && (
                <li>
                  <a href={`tel:${settings.phone}`} className="hover:underline" style={{ color: theme.colors.text.secondary }}>
                    {settings.phone}
                  </a>
                </li>
              )}
              <li>
                <a href="/admin" className="hover:underline" style={{ color: theme.colors.text.secondary }}>
                  Administration
                </a>
              </li>
            </ul>
          </div>
        </div>

        <div className="border-t pt-8" style={{ borderColor: theme.colors.text.light + '30' }}>
          <div className="flex flex-col md:flex-row justify-between items-center">
            <p className="text-sm" style={{ color: theme.colors.text.secondary }}>
              © {new Date().getFullYear()} {settings?.siteName}.
            </p>
          </div>
        </div>
      </div>
    </footer>
  );
};

// Youtube Footer - Video platform style
export const YoutubeFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  return (
    <footer className="bg-gray-100" style={{ backgroundColor: theme.colors.surface }}>
      <div className="container mx-auto px-6 py-16">
        <div className="grid md:grid-cols-4 gap-8 mb-12">
          <div className="md:col-span-2">
            <h3 className="text-3xl font-bold mb-6" style={{ color: theme.colors.primary }}>
              {settings?.logoText || ''}
            </h3>
            <p className="mb-8" style={{ color: theme.colors.text.secondary }}>
              {settings?.footerText || ''}
            </p>
            <div className="flex gap-4">
<SocialLinks customStyles={true} size={20} iconStyle={{ color: theme.colors.text.secondary }} />
              {navigationItems?.map((item) => (
                <li key={item.id}>
                  <button 
                    onClick={() => scrollToSection?.(item.type)} 
                    className="hover:underline text-left"
                    style={{ color: theme.colors.text.secondary }}
                  >
                    {item.label}
                  </button>
                </li>
              ))}
            </ul>
          </div>

          <div>
            <h4 className="font-semibold mb-6" style={{ color: theme.colors.text.primary }}>Contact</h4>
            <ul className="space-y-3">
              {settings?.email && (
                <li>
                  <a href={`mailto:${settings.email}`} className="hover:underline" style={{ color: theme.colors.text.secondary }}>
                    {settings.email}
                  </a>
                </li>
              )}
              {settings?.phone && (
                <li>
                  <a href={`tel:${settings.phone}`} className="hover:underline" style={{ color: theme.colors.text.secondary }}>
                    {settings.phone}
                  </a>
                </li>
              )}
              <li>
                <a href="/admin" className="hover:underline" style={{ color: theme.colors.text.secondary }}>
                  Administration
                </a>
              </li>
            </ul>
          </div>
        </div>

        <div className="border-t pt-8" style={{ borderColor: theme.colors.text.light + '30' }}>
          <div className="flex flex-col md:flex-row justify-between items-center">
            <p className="text-sm" style={{ color: theme.colors.text.light }}>
              © {new Date().getFullYear()} {settings?.siteName}.
            </p>
          </div>
        </div>
      </div>
    </footer>
  );
};

// Medium Footer - Publishing platform style
export const MediumFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  return (
    <footer style={{ backgroundColor: theme.colors.surface }}>
      <div className="container mx-auto px-6 py-16">
        <div className="grid md:grid-cols-4 gap-8 mb-12">
          <div className="md:col-span-2">
            <h3 className="text-3xl font-bold mb-6" style={{ color: theme.colors.text.primary }}>
              {settings?.siteName}
            </h3>
            <p className="mb-8" style={{ color: theme.colors.text.secondary }}>
              {settings?.footerText || ''}
            </p>
            <div className="flex gap-4">
<SocialLinks customStyles={true} size={20} iconStyle={{ color: theme.colors.text.secondary }} />
                    style={{ color: theme.colors.text.secondary }}
                  >
                    {item.label}
                  </button>
                </li>
              ))}
            </ul>
          </div>

          <div>
            <h4 className="font-medium mb-6" style={{ color: theme.colors.text.primary }}>Contact</h4>
            <ul className="space-y-3">
              {settings?.email && (
                <li>
                  <a href={`mailto:${settings.email}`} className="text-sm hover:underline" style={{ color: theme.colors.text.secondary }}>
                    {settings.email}
                  </a>
                </li>
              )}
              {settings?.phone && (
                <li>
                  <a href={`tel:${settings.phone}`} className="text-sm hover:underline" style={{ color: theme.colors.text.secondary }}>
                    {settings.phone}
                  </a>
                </li>
              )}
              <li>
                <a href="/admin" className="text-sm hover:underline" style={{ color: theme.colors.text.secondary }}>
                  Administration
                </a>
              </li>
            </ul>
          </div>
        </div>

        <div className="border-t pt-8" style={{ borderColor: theme.colors.text.light + '30' }}>
          <p className="text-sm" style={{ color: theme.colors.text.light }}>
            © {new Date().getFullYear()} {settings?.siteName}.
          </p>
        </div>
      </div>
    </footer>
  );
};

// Twitch Footer - Gaming/streaming style
export const TwitchFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  return (
    <footer className="bg-gray-900 text-white">
      <div className="container mx-auto px-6 py-16">
        <div className="grid md:grid-cols-4 gap-8 mb-12">
          <div className="md:col-span-2">
            <h3 className="text-4xl font-bold mb-6" style={{ color: theme.colors.primary }}>
              {settings?.logoText || ''}
            </h3>
            <p className="text-gray-300 text-lg mb-8">
              {settings?.footerText || ''}
            </p>
            <div className="flex gap-4">
<SocialLinks customStyles={true} size={20} iconStyle={{ color: theme.colors.text.secondary }} />
              {navigationItems?.map((item) => (
                <li key={item.id}>
                  <button 
                    onClick={() => scrollToSection?.(item.type)} 
                    className="text-gray-400 hover:text-white transition-colors text-left"
                  >
                    {item.label}
                  </button>
                </li>
              ))}
            </ul>
          </div>

          <div>
            <h4 className="font-semibold mb-6 text-white">Contact</h4>
            <ul className="space-y-3">
              {settings?.email && (
                <li>
                  <a href={`mailto:${settings.email}`} className="text-gray-400 hover:text-white transition-colors">
                    {settings.email}
                  </a>
                </li>
              )}
              {settings?.phone && (
                <li>
                  <a href={`tel:${settings.phone}`} className="text-gray-400 hover:text-white transition-colors">
                    {settings.phone}
                  </a>
                </li>
              )}
              <li>
                <a href="/admin" className="text-gray-400 hover:text-white transition-colors">
                  Administration
                </a>
              </li>
            </ul>
          </div>
        </div>

        <div className="border-t border-gray-800 pt-8">
          <div className="flex flex-col md:flex-row justify-between items-center">
            <p className="text-gray-400">
              © {new Date().getFullYear()} {settings?.siteName}.
            </p>
          </div>
        </div>
      </div>
    </footer>
  );
};

// Instagram Footer - Photo sharing style
export const InstagramFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  return (
    <footer style={{ backgroundColor: theme.colors.surface }}>
      <div className="container mx-auto px-6 py-16">
        <div className="grid md:grid-cols-5 gap-8 mb-12">
          <div className="md:col-span-2">
            <div className="flex items-center gap-3 mb-6">
              {settings?.logoUrl ? (
                <img 
                  src={settings.logoUrl} 
                  alt={settings.siteName || 'Logo'} 
                  className="h-12 w-auto object-contain"
                />
              ) : (
                <div className="w-12 h-12 rounded-xl bg-gradient-to-r from-purple-500 to-pink-500">
                  <span className="text-white font-bold text-xl flex items-center justify-center h-full">
                    {settings?.siteName ? settings.siteName.substring(0, 2) : ''}
                  </span>
                </div>
              )}
              <h3 className="text-3xl font-bold" style={{ color: theme.colors.text.primary }}>
                {settings?.siteName}
              </h3>
            </div>
            <p className="mb-8" style={{ color: theme.colors.text.secondary }}>
              {settings?.footerText || ''}
            </p>
            <div className="flex gap-4">
<SocialLinks customStyles={true} size={20} iconStyle={{ color: theme.colors.text.secondary }} />
                    style={{ color: theme.colors.text.secondary }}
                  >
                    {item.label}
                  </button>
                </li>
              ))}
            </ul>
          </div>

          <div>
            <h4 className="font-semibold mb-6" style={{ color: theme.colors.text.primary }}>Contact</h4>
            <ul className="space-y-3">
              {settings?.email && (
                <li>
                  <a href={`mailto:${settings.email}`} className="hover:underline" style={{ color: theme.colors.text.secondary }}>
                    {settings.email}
                  </a>
                </li>
              )}
              {settings?.phone && (
                <li>
                  <a href={`tel:${settings.phone}`} className="hover:underline" style={{ color: theme.colors.text.secondary }}>
                    {settings.phone}
                  </a>
                </li>
              )}
              <li>
                <a href="/admin" className="hover:underline" style={{ color: theme.colors.text.secondary }}>
                  Administration
                </a>
              </li>
            </ul>
          </div>
        </div>

        <div className="border-t pt-8" style={{ borderColor: theme.colors.text.light + '30' }}>
          <p className="text-center" style={{ color: theme.colors.text.secondary }}>
            © {new Date().getFullYear()} {settings?.siteName}.
          </p>
        </div>
      </div>
    </footer>
  );
};

// WhatsApp Footer - Messaging app style
export const WhatsAppFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  return (
    <footer className="bg-green-50" style={{ backgroundColor: theme.colors.surface }}>
      <div className="container mx-auto px-6 py-16">
        <div className="grid md:grid-cols-4 gap-8 mb-12">
          <div className="md:col-span-2">
            <div className="flex items-center gap-3 mb-6">
              {settings?.logoUrl ? (
                <img 
                  src={settings.logoUrl} 
                  alt={settings.siteName || 'Logo'} 
                  className="h-12 w-auto object-contain"
                />
              ) : (
                <div className="w-12 h-12 rounded-full bg-green-500">
                  <span className="text-white font-bold text-xl flex items-center justify-center h-full">
                    {settings?.siteName ? settings.siteName.charAt(0) : ''}
                  </span>
                </div>
              )}
              <h3 className="text-3xl font-bold" style={{ color: theme.colors.text.primary }}>
                {settings?.siteName}
              </h3>
            </div>
            <p className="mb-8" style={{ color: theme.colors.text.secondary }}>
              {settings?.footerText || ''}
            </p>
            <div className="flex gap-4">
<SocialLinks customStyles={true} size={20} iconStyle={{ color: theme.colors.text.secondary }} />
                  >
                    {item.label}
                  </button>
                </li>
              ))}
            </ul>
          </div>

          <div>
            <h4 className="font-semibold mb-6" style={{ color: theme.colors.text.primary }}>Contact</h4>
            <ul className="space-y-3">
              {settings?.email && (
                <li>
                  <a href={`mailto:${settings.email}`} className="text-green-700 hover:text-green-800 transition-colors">
                    {settings.email}
                  </a>
                </li>
              )}
              {settings?.phone && (
                <li>
                  <a href={`tel:${settings.phone}`} className="text-green-700 hover:text-green-800 transition-colors">
                    {settings.phone}
                  </a>
                </li>
              )}
              <li>
                <a href="/admin" className="text-green-700 hover:text-green-800 transition-colors">
                  Administration
                </a>
              </li>
            </ul>
          </div>
        </div>

        <div className="border-t pt-8 border-green-200">
          <div className="flex flex-col md:flex-row justify-between items-center">
            <p className="text-green-600">
              © {new Date().getFullYear()} {settings?.siteName}.
            </p>
          </div>
        </div>
      </div>
    </footer>
  );
};

// Behance Footer - Creative portfolio style
export const BehanceFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  return (
    <footer style={{ backgroundColor: theme.colors.surface }}>
      <div className="container mx-auto px-6 py-16">
        <div className="grid md:grid-cols-5 gap-8 mb-12">
          <div className="md:col-span-2">
            <h3 className="text-4xl font-bold mb-6" style={{ color: theme.colors.primary }}>
              {settings?.logoText || ''}
            </h3>
            <p className="mb-8 text-lg" style={{ color: theme.colors.text.secondary }}>
              {settings?.footerText || ''}
            </p>
            <div className="flex gap-4">
<SocialLinks customStyles={true} size={20} iconStyle={{ color: theme.colors.text.secondary }} />
              {navigationItems?.map((item) => (
                <li key={item.id}>
                  <button 
                    onClick={() => scrollToSection?.(item.type)} 
                    className="hover:underline text-left"
                    style={{ color: theme.colors.text.secondary }}
                  >
                    {item.label}
                  </button>
                </li>
              ))}
            </ul>
          </div>

          <div>
            <h4 className="font-semibold mb-6" style={{ color: theme.colors.text.primary }}>Contact</h4>
            <ul className="space-y-3">
              {settings?.email && (
                <li>
                  <a href={`mailto:${settings.email}`} className="hover:underline" style={{ color: theme.colors.text.secondary }}>
                    {settings.email}
                  </a>
                </li>
              )}
              {settings?.phone && (
                <li>
                  <a href={`tel:${settings.phone}`} className="hover:underline" style={{ color: theme.colors.text.secondary }}>
                    {settings.phone}
                  </a>
                </li>
              )}
              <li>
                <a href="/admin" className="hover:underline" style={{ color: theme.colors.text.secondary }}>
                  Administration
                </a>
              </li>
            </ul>
          </div>
        </div>

        <div className="border-t pt-8" style={{ borderColor: theme.colors.text.light + '30' }}>
          <div className="flex flex-col md:flex-row justify-between items-center">
            <p className="text-sm" style={{ color: theme.colors.text.secondary }}>
              © {new Date().getFullYear()} {settings?.siteName}.
            </p>
          </div>
        </div>
      </div>
    </footer>
  );
};

// Uber Footer - Transportation service style
export const UberFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  return (
    <footer className="bg-black text-white">
      <div className="container mx-auto px-6 py-16">
        <div className="grid md:grid-cols-4 gap-8 mb-12">
          <div className="md:col-span-2">
            <h3 className="text-4xl font-bold mb-6" style={{ color: theme.colors.primary }}>
              {settings?.logoText || ''}
            </h3>
            <p className="text-gray-300 text-lg mb-8">
              {settings?.footerText || ''}
            </p>
            <div className="flex gap-4">
<SocialLinks customStyles={true} size={20} iconStyle={{ color: theme.colors.text.secondary }} />
              {navigationItems?.map((item) => (
                <li key={item.id}>
                  <button 
                    onClick={() => scrollToSection?.(item.type)} 
                    className="text-gray-400 hover:text-white transition-colors text-left"
                  >
                    {item.label}
                  </button>
                </li>
              ))}
            </ul>
          </div>

          <div>
            <h4 className="font-semibold mb-6 text-white">Contact</h4>
            <ul className="space-y-3">
              {settings?.email && (
                <li>
                  <a href={`mailto:${settings.email}`} className="text-gray-400 hover:text-white transition-colors">
                    {settings.email}
                  </a>
                </li>
              )}
              {settings?.phone && (
                <li>
                  <a href={`tel:${settings.phone}`} className="text-gray-400 hover:text-white transition-colors">
                    {settings.phone}
                  </a>
                </li>
              )}
              <li>
                <a href="/admin" className="text-gray-400 hover:text-white transition-colors">
                  Administration
                </a>
              </li>
            </ul>
          </div>
        </div>

        <div className="border-t border-gray-800 pt-8">
          <div className="flex flex-col md:flex-row justify-between items-center">
            <p className="text-gray-400">
              © {new Date().getFullYear()} {settings?.siteName}.
            </p>
          </div>
        </div>
      </div>
    </footer>
  );
};

// Cursor Footer - AI coding style
export const CursorFooter = ({ theme, settings, navigationItems, scrollToSection }) => {
  return (
    <footer className="bg-gray-900 text-white">
      <div className="container mx-auto px-6 py-16">
        <div className="grid md:grid-cols-4 gap-8 mb-12">
          <div className="md:col-span-2">
            <div className="flex items-center gap-3 mb-6">
              {settings?.logoUrl ? (
                <img 
                  src={settings.logoUrl} 
                  alt={settings.siteName || 'Logo'} 
                  className="h-10 w-auto object-contain"
                />
              ) : (
                <div className="w-10 h-10 rounded-lg bg-gradient-to-r from-blue-500 to-purple-600">
                  <span className="text-white font-bold text-lg flex items-center justify-center h-full">
                    {settings?.siteName ? settings.siteName.charAt(0) : ''}
                  </span>
                </div>
              )}
              <h3 className="text-3xl font-bold" style={{ color: theme.colors.primary }}>
                {settings?.siteName}
              </h3>
            </div>
            <p className="text-gray-300 mb-8">
              {settings?.footerText || ''}
            </p>
            <div className="flex gap-4">
<SocialLinks customStyles={true} size={20} iconStyle={{ color: theme.colors.text.secondary }} />
                  >
                    {item.label}
                  </button>
                </li>
              ))}
            </ul>
          </div>

          <div>
            <h4 className="font-semibold mb-6 text-white">Contact</h4>
            <ul className="space-y-3">
              {settings?.email && (
                <li>
                  <a href={`mailto:${settings.email}`} className="text-gray-400 hover:text-white transition-colors">
                    {settings.email}
                  </a>
                </li>
              )}
              {settings?.phone && (
                <li>
                  <a href={`tel:${settings.phone}`} className="text-gray-400 hover:text-white transition-colors">
                    {settings.phone}
                  </a>
                </li>
              )}
              <li>
                <a href="/admin" className="text-gray-400 hover:text-white transition-colors">
                  Administration
                </a>
              </li>
            </ul>
          </div>
        </div>

        <div className="border-t border-gray-800 pt-8">
          <p className="text-center text-gray-400">
            © {new Date().getFullYear()} {settings?.siteName}.
          </p>
        </div>
      </div>
    </footer>
  );
};